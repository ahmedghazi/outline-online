// import React from "react";
// // import { SanityVideo } from '../../../graphql-types';
// import { VideoWrapper } from "@/app/components/ui/player";
// import { Embed } from "@/app/types/schema";
// // import { VideoWrapper } from 'components/ui/player';

// type Props = {
//   input: {
//     embed: Embed;
//   };
// };

// const EmbedUI = ({ input }: Props) => {
//   return (
//     <section className='module module--embed w-full mb-xl'>
//       {input.embed.url && (
//         <VideoWrapper url={input.embed.url} showControls={true} />
//       )}
//     </section>
//   );
// };

// export default EmbedUI;
